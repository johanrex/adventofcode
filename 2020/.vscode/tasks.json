{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build current file (MSVC)",
            "type": "process",
            "command": "${workspaceFolder}\\.vscode\\msvc_build.bat",
            "args": [
                "${file}"
            ],
            "problemMatcher": ["$msCompile"],
            "group": { "kind": "build", "isDefault": true }
        }
    ]
}